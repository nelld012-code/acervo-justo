name: Verify financeiro report patch

on:
  push:
    branches:
      - fix/financeiro-relatorios-actions

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Apply patch
        run: python scripts/patch_financeiro_relatorios.py
      - name: Validate build
        run: |
          npm install
          npm run build
      - name: Commit result and clean temporary files
        run: |
          rm -f scripts/patch_financeiro_relatorios.py .github/workflows/verify-financeiro-patch.yml .github/workflows/run-financeiro-patch.yml .github/workflows/one-shot-financeiro-actions.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/routes/_authenticated/financeiro.tsx scripts/patch_financeiro_relatorios.py .github/workflows/verify-financeiro-patch.yml .github/workflows/run-financeiro-patch.yml .github/workflows/one-shot-financeiro-actions.yml
          git commit -m "Adiciona ações aos relatórios financeiros"
          git push origin HEAD:fix/financeiro-relatorios-actions
