import { createFileRoute } from "@tanstack/react-router";
import { useMemo, useRef, useState } from "react";
import { useQuery, useQueryClient } from "@tanstack/react-query";
import { supabase } from "@/integrations/supabase/client";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from "@/components/ui/alert-dialog";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Plus, Pencil, Trash2, Printer, MessageCircle, Upload, FileSpreadsheet, X } from "lucide-react";
import { toast } from "sonner";
import { printReport } from "@/lib/print-report";
import { whatsappLink } from "@/lib/documents";
import { parseReceptionExcel, type ReceptionImportRow, type ReceptionImportError } from "@/lib/reception-import";
import { StandardPagination } from "@/components/standard-pagination";
import { WeeklyAgenda } from "@/components/weekly-agenda";

export const Route = createFileRoute("/_authenticated/recepcao")({ head: () => ({ meta: [{ title: "Recepção - Gestão Judicial" }, { name: "description", content: "Registro de atendimentos da recepção: data, hora, advogado, cliente, CPF, telefone e atendente." }, { property: "og:title", content: "Recepção - Gestão Judicial" }, { property: "og:description", content: "Cadastre e consulte os atendimentos recebidos na recepção." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary" }] }), component: RecepcaoPage });

type Reception = { id: string; data: string; hora: string | null; hora_saida: string | null; advogado: string; nome_cliente: string; cpf: string | null; telefone: string; atendente: string; assunto: string | null; atestado_emitido: boolean; atestado_emitido_em: string | null; created_at: string };
const emptyForm = { data: new Date().toISOString().slice(0, 10), hora: "", hora_saida: "", advogado: "", nome_cliente: "", cpf: "", telefone: "", atendente: "", assunto: "" };
const PAGE_SIZE = 8;
function br(d: string) { if (!d) return "—"; const [y, m, dd] = d.split("-"); return `${dd}/${m}/${y}`; }
function iso(d: Date) { return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, "0")}-${String(d.getDate()).padStart(2, "0")}`; }
function weekRange() { const now = new Date(); const day = (now.getDay() + 6) % 7; const start = new Date(now); start.setDate(now.getDate() - day); const end = new Date(start); end.setDate(start.getDate() + 6); return { start: iso(start), end: iso(end) }; }
const ADVOGADOS_PADRAO = ["Dr. Dimas", "Dra Cassia", "Dr. Wesley"];

function RecepcaoPage() {
  const qc = useQueryClient(); const [open, setOpen] = useState(false); const [editing, setEditing] = useState<Reception | null>(null); const [form, setForm] = useState({ ...emptyForm }); const [saving, setSaving] = useState(false); const [toDelete, setToDelete] = useState<Reception | null>(null); const [busca, setBusca] = useState("");
  const [page, setPage] = useState(1);
  const [periodo, setPeriodo] = useState<"todos" | "hoje" | "semana">("todos");
  const [advogadoFiltro, setAdvogadoFiltro] = useState("todos");
  const [atendenteFiltro, setAtendenteFiltro] = useState("todos");
  const [dataDe, setDataDe] = useState(""); const [dataAte, setDataAte] = useState("");
  const [importOpen, setImportOpen] = useState(false); const [importRows, setImportRows] = useState<ReceptionImportRow[]>([]); const [importErrors, setImportErrors] = useState<ReceptionImportError[]>([]); const [importing, setImporting] = useState(false); const [importPage, setImportPage] = useState(1); const fileRef = useRef<HTMLInputElement>(null);
  const { data: rows = [], isLoading } = useQuery({ queryKey: ["reception-entries"], queryFn: async () => { const { data, error } = await supabase.from("reception_entries").select("*").order("data", { ascending: false }).order("created_at", { ascending: false }); if (error) throw error; return data as Reception[]; } });
  const hojeIso = iso(new Date());
  const semana = useMemo(() => weekRange(), []);
  const counts = useMemo(() => ({ hoje: rows.filter(r => r.data === hojeIso).length, semana: rows.filter(r => r.data >= semana.start && r.data <= semana.end).length, total: rows.length }), [rows, hojeIso, semana]);
  const advogadosOpcoes = useMemo(() => {
    const doBanco = rows.map(r => r.advogado?.trim()).filter((v): v is string => Boolean(v));
    const vistos = new Set(ADVOGADOS_PADRAO);
    const extras = doBanco.filter(a => !vistos.has(a));
    return Array.from(new Set([...ADVOGADOS_PADRAO, ...extras])).sort((a, b) => a.localeCompare(b));
  }, [rows]);
  const atendentesOpcoes = useMemo(() => Array.from(new Set(rows.map(r => r.atendente).filter(Boolean))).sort((a, b) => a.localeCompare(b)), [rows]);
  const filtered = useMemo(() => {
    const q = busca.trim().toLowerCase();
    return rows.filter(r => {
      if (q && ![r.nome_cliente, r.advogado, r.atendente, r.cpf ?? "", r.telefone].some(v => v.toLowerCase().includes(q))) return false;
      if (periodo === "hoje" && r.data !== hojeIso) return false;
      if (periodo === "semana" && !(r.data >= semana.start && r.data <= semana.end)) return false;
      if (advogadoFiltro !== "todos" && r.advogado !== advogadoFiltro) return false;
      if (atendenteFiltro !== "todos" && r.atendente !== atendenteFiltro) return false;
      if (dataDe && r.data < dataDe) return false;
      if (dataAte && r.data > dataAte) return false;
      return true;
    });
  }, [rows, busca, periodo, advogadoFiltro, atendenteFiltro, dataDe, dataAte, hojeIso, semana]);
  function limparFiltros() { setBusca(""); setPeriodo("todos"); setAdvogadoFiltro("todos"); setAtendenteFiltro("todos"); setDataDe(""); setDataAte(""); setPage(1); }
  const totalPages = Math.max(1, Math.ceil(filtered.length / PAGE_SIZE));
  const safePage = Math.min(page, totalPages);
  const pagedRows = filtered.slice((safePage - 1) * PAGE_SIZE, safePage * PAGE_SIZE);
  const importTotalPages = Math.max(1, Math.ceil(importRows.length / PAGE_SIZE));
  const safeImportPage = Math.min(importPage, importTotalPages);
  const pagedImportRows = importRows.slice((safeImportPage - 1) * PAGE_SIZE, safeImportPage * PAGE_SIZE);
  function openNew() { setEditing(null); setForm({ ...emptyForm }); setOpen(true); }
  function openEdit(r: Reception) { setEditing(r); setForm({ data: r.data, hora: r.hora ?? "", advogado: r.advogado, nome_cliente: r.nome_cliente, cpf: r.cpf ?? "", telefone: r.telefone, atendente: r.atendente }); setOpen(true); }
  async function save() { if (!form.nome_cliente.trim()) return toast.error("Informe o nome do cliente"); if (!form.telefone.trim()) return toast.error("Informe o telefone"); if (!form.advogado.trim()) return toast.error("Informe o advogado"); if (!form.atendente.trim()) return toast.error("Informe o atendente"); setSaving(true); try { const payload = { data: form.data, hora: form.hora || null, hora_saida: form.hora_saida || null, advogado: form.advogado.trim(), nome_cliente: form.nome_cliente.trim(), cpf: form.cpf.trim() || null, telefone: form.telefone.trim(), atendente: form.atendente.trim(), assunto: form.assunto.trim() || null }; if (editing) { const { error } = await supabase.from("reception_entries").update(payload).eq("id", editing.id); if (error) throw error; toast.success("Registro atualizado"); } else { const { data: auth } = await supabase.auth.getUser(); const { error } = await supabase.from("reception_entries").insert({ ...payload, created_by: auth.user?.id ?? null }); if (error) throw error; toast.success("Atendimento registrado"); } setOpen(false); qc.invalidateQueries({ queryKey: ["reception-entries"] }); } catch (e) { toast.error("Erro ao salvar", { description: e instanceof Error ? e.message : "" }); } finally { setSaving(false); } }
  async function confirmDelete() { if (!toDelete) return; const { error } = await supabase.from("reception_entries").delete().eq("id", toDelete.id); if (error) toast.error("Erro ao excluir", { description: error.message }); else { toast.success("Registro excluído"); qc.invalidateQueries({ queryKey: ["reception-entries"] }); } setToDelete(null); }
  function printOne(r: Reception) { printReport({ title: "Registro de Recepção", subtitle: r.nome_cliente, sections: [{ heading: "Dados do atendimento", columns: ["Campo", "Informação"], rows: [["Data", br(r.data)], ["Hora de chegada", r.hora || "—"], ["Hora de saída", r.hora_saida || "—"], ["Advogado", r.advogado], ["Nome do cliente", r.nome_cliente], ["CPF", r.cpf || "—"], ["Telefone", r.telefone], ["Atendente", r.atendente], ["Assunto", r.assunto || "—"]] }] }); }
  function printList() { printReport({ title: "Recepção — Registros de Atendimento", subtitle: `${filtered.length} registro(s)`, sections: [{ columns: ["Data", "Hora", "Advogado", "Nome do cliente", "CPF", "Telefone", "Atendente"], rows: filtered.map(r => [br(r.data), r.hora || "—", r.advogado, r.nome_cliente, r.cpf || "—", r.telefone, r.atendente]) }] }); }
  function gerarAtestado(r: Reception) { printReport({ title: "ATESTADO DE COMPARECIMENTO", subtitle: "J DIMAS GONÇALVES · ESCRITORIO DE ADVOCACIA", sections: [{ columns: ["Campo","Informação"], rows: [["Nome", r.nome_cliente], ["CPF", r.cpf || "—"], ["Data do comparecimento", br(r.data)], ["Hora de chegada", r.hora || "—"], ["Hora de saída", r.hora_saida || "—"], ["Assunto", r.assunto || "atendimento profissional"], ["Declaração", "Atestamos, para os devidos fins, que a pessoa acima identificada compareceu a este escritório para tratar do assunto informado."]], }] }); void supabase.from("reception_entries").update({ atestado_emitido: true, atestado_emitido_em: new Date().toISOString() }).eq("id", r.id); }
  async function chooseImport(file?: File) { if (!file) return; try { const result = await parseReceptionExcel(file); setImportRows(result.rows); setImportErrors(result.errors); setImportPage(1); setImportOpen(true); } catch (e) { toast.error("Não foi possível ler a planilha", { description: e instanceof Error ? e.message : "Arquivo inválido" }); } finally { if (fileRef.current) fileRef.current.value = ""; } }
  async function confirmImport() { if (!importRows.length) return; setImporting(true); try { const { data: auth } = await supabase.auth.getUser(); const payload = importRows.map(r => ({ ...r, created_by: auth.user?.id ?? null })); const { error } = await supabase.from("reception_entries").insert(payload); if (error) throw error; toast.success(`${importRows.length} registro(s) importado(s) com sucesso`); setImportOpen(false); setImportRows([]); setImportErrors([]); qc.invalidateQueries({ queryKey: ["reception-entries"] }); } catch (e) { toast.error("Erro ao importar", { description: e instanceof Error ? e.message : "Não foi possível salvar os registros" }); } finally { setImporting(false); } }
    return <div className="min-w-0 space-y-4">
    <div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div className="min-w-0"><h1 className="text-xl font-semibold text-foreground sm:text-2xl">Recepção</h1><p className="text-sm text-muted-foreground">Registro de atendimentos recebidos na recepção.</p></div><div className="flex flex-wrap gap-2"><Button variant="outline" onClick={() => fileRef.current?.click()} className="flex-1 sm:flex-none"><Upload className="mr-2 h-4 w-4" /> Importar Excel</Button><Button variant="outline" onClick={printList} className="flex-1 sm:flex-none"><Printer className="mr-2 h-4 w-4" /> Imprimir lista</Button><Button onClick={openNew} className="flex-1 sm:flex-none"><Plus className="mr-2 h-4 w-4" /> Novo registro</Button><input ref={fileRef} type="file" accept=".xlsx,.xls" className="hidden" onChange={e => void chooseImport(e.target.files?.[0])} /></div></div>
    <div className="grid gap-3 sm:grid-cols-3">
      {[{ label: "Hoje", value: counts.hoje }, { label: "Esta semana", value: counts.semana }, { label: "Total", value: counts.total }].map(c => <Card key={c.label}><CardContent className="p-4"><div className="text-xs uppercase text-muted-foreground">{c.label}</div><div className="text-2xl font-semibold">{c.value}</div></CardContent></Card>)}
    </div>
    <section aria-labelledby="agenda-semanal-titulo" className="min-w-0">
      <div className="mb-3 flex items-center justify-between gap-3 px-1">
        <div>
          <h2 id="agenda-semanal-titulo" className="text-lg font-semibold text-foreground sm:text-xl">Agenda Semanal</h2>
          <p className="text-sm text-muted-foreground">Atividades e compromissos da equipe.</p>
        </div>
      </div>
      <div className="min-w-0 w-full">
        <WeeklyAgenda />
      </div>
    </section>
    <Card><CardContent className="space-y-3 p-4">
      <Input placeholder="Buscar por cliente, advogado, atendente, CPF ou telefone" value={busca} onChange={e => { setBusca(e.target.value); setPage(1); }} />
      <div className="flex flex-wrap gap-2">
        {([["todos", "Todos"], ["hoje", "Hoje"], ["semana", "Esta semana"]] as const).map(([v, l]) => <Button key={v} size="sm" variant={periodo === v ? "default" : "outline"} onClick={() => { setPeriodo(v); setPage(1); }}>{l}</Button>)}
      </div>
      <div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">
        <div className="space-y-1.5"><Label>Advogado</Label><Select value={advogadoFiltro} onValueChange={v => { setAdvogadoFiltro(v); setPage(1); }}><SelectTrigger><SelectValue placeholder="Todos" /></SelectTrigger><SelectContent><SelectItem value="todos">Todos</SelectItem>{advogadosOpcoes.map(a => <SelectItem key={a} value={a}>{a}</SelectItem>)}</SelectContent></Select></div>
        <div className="space-y-1.5"><Label>Atendente</Label><Select value={atendenteFiltro} onValueChange={v => { setAtendenteFiltro(v); setPage(1); }}><SelectTrigger><SelectValue placeholder="Todos" /></SelectTrigger><SelectContent><SelectItem value="todos">Todos</SelectItem>{atendentesOpcoes.map(a => <SelectItem key={a} value={a}>{a}</SelectItem>)}</SelectContent></Select></div>
        <div className="space-y-1.5"><Label>Data desde</Label><Input type="date" value={dataDe} onChange={e => { setDataDe(e.target.value); setPage(1); }} /></div>
        <div className="space-y-1.5"><Label>Data até</Label><Input type="date" value={dataAte} onChange={e => { setDataAte(e.target.value); setPage(1); }} /></div>
      </div>
      <div className="flex justify-end"><Button variant="ghost" size="sm" onClick={limparFiltros}><X className="mr-1 h-4 w-4" /> Limpar filtros</Button></div>
    </CardContent></Card>
    <Card><CardHeader className="pb-2"><CardTitle className="text-base">Atendimentos ({filtered.length})</CardTitle></CardHeader><CardContent className="p-0 sm:p-6 sm:pt-0">{isLoading ? <p className="p-4 text-sm text-muted-foreground">Carregando...</p> : filtered.length === 0 ? <p className="p-4 text-sm text-muted-foreground">Nenhum registro encontrado.</p> : <><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left text-xs uppercase text-muted-foreground"><th className="p-2">Data</th><th className="p-2">Hora</th><th className="p-2">Saída</th><th className="p-2">Advogado</th><th className="p-2">Nome do cliente</th><th className="p-2">CPF</th><th className="p-2">Telefone</th><th className="p-2">Atendente</th><th className="p-2 text-right">Ações</th></tr></thead><tbody>{pagedRows.map(r => <tr key={r.id} className="border-b last:border-0"><td className="p-2 whitespace-nowrap">{br(r.data)}</td><td className="p-2 whitespace-nowrap">{r.hora || "—"}</td><td className="p-2 whitespace-nowrap">{r.hora_saida || "—"}</td><td className="p-2">{r.advogado}</td><td className="p-2 font-medium">{r.nome_cliente}</td><td className="p-2">{r.cpf || "—"}</td><td className="p-2 whitespace-nowrap"><a href={whatsappLink(r.telefone)} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 text-primary hover:underline"><MessageCircle className="h-4 w-4" />{r.telefone}</a></td><td className="p-2">{r.atendente}</td><td className="p-2"><div className="flex justify-end gap-1"><Button size="icon" variant="ghost" title="Editar" onClick={() => openEdit(r)}><Pencil className="h-4 w-4" /></Button><Button size="icon" variant="ghost" title="Excluir" onClick={() => setToDelete(r)}><Trash2 className="h-4 w-4 text-destructive" /></Button><Button size="icon" variant="ghost" title="Imprimir" onClick={() => printOne(r)}><Printer className="h-4 w-4" /></Button></div></td></tr>)}</tbody></table></div><StandardPagination current={safePage} total={totalPages} totalItems={filtered.length} pageSize={PAGE_SIZE} onChange={setPage} /></>}</CardContent></Card>
    <Dialog open={open} onOpenChange={setOpen}><DialogContent className="max-h-[85vh] w-[calc(100vw-2rem)] overflow-y-auto sm:max-w-lg"><DialogHeader><DialogTitle>{editing ? "Editar registro" : "Novo registro de atendimento"}</DialogTitle><DialogDescription>Preencha os dados do atendimento da recepção.</DialogDescription></DialogHeader><div className="grid gap-3 sm:grid-cols-2"><div className="space-y-1.5"><Label>Data</Label><Input type="date" value={form.data} onChange={e => setForm({ ...form, data: e.target.value })} /></div><div className="space-y-1.5"><Label>Hora</Label><Input type="time" value={form.hora} onChange={e => setForm({ ...form, hora: e.target.value })} /></div><div className="space-y-1.5 sm:col-span-2"><Label>Advogado</Label><Input value={form.advogado} onChange={e => setForm({ ...form, advogado: e.target.value })} /></div><div className="space-y-1.5 sm:col-span-2"><Label>Nome do cliente</Label><Input value={form.nome_cliente} onChange={e => setForm({ ...form, nome_cliente: e.target.value })} /></div><div className="space-y-1.5"><Label>CPF</Label><Input value={form.cpf} onChange={e => setForm({ ...form, cpf: e.target.value })} /></div><div className="space-y-1.5"><Label>Telefone</Label><Input value={form.telefone} onChange={e => setForm({ ...form, telefone: e.target.value })} /></div><div className="space-y-1.5"><Label>Hora de saída</Label><Input type="time" value={form.hora_saida} onChange={e => setForm({ ...form, hora_saida: e.target.value })} /></div><div className="space-y-1.5 sm:col-span-2"><Label>Assunto</Label><Input value={form.assunto} onChange={e => setForm({ ...form, assunto: e.target.value })} placeholder="Motivo ou assunto do atendimento" /></div><div className="space-y-1.5 sm:col-span-2"><Label>Atendente</Label><Input value={form.atendente} onChange={e => setForm({ ...form, atendente: e.target.value })} /></div></div><DialogFooter><Button variant="outline" onClick={() => setOpen(false)}>Cancelar</Button><Button onClick={save} disabled={saving}>{saving ? "Salvando..." : "Salvar"}</Button></DialogFooter></DialogContent></Dialog>
    <Dialog open={importOpen} onOpenChange={v => !importing && setImportOpen(v)}><DialogContent className="h-[850px] max-h-[90vh] w-[calc(100vw-2rem)] overflow-hidden sm:max-w-6xl"><DialogHeader className="shrink-0"><DialogTitle className="flex items-center gap-2"><FileSpreadsheet className="h-5 w-5" /> Prévia da importação</DialogTitle><DialogDescription>Formato: Data | Advogado | Nome do cliente | CPF | Telefone | Atendente.</DialogDescription></DialogHeader><div className="min-h-0 flex-1 space-y-3 overflow-hidden"><div className="grid gap-2 sm:grid-cols-3"><div className="rounded-md border p-3"><div className="text-xs text-muted-foreground">Válidos</div><div className="text-lg font-semibold">{importRows.length}</div></div><div className="rounded-md border p-3"><div className="text-xs text-muted-foreground">Com erro</div><div className="text-lg font-semibold">{importErrors.length}</div></div><div className="rounded-md border p-3"><div className="text-xs text-muted-foreground">Total processado</div><div className="text-lg font-semibold">{importRows.length + importErrors.length}</div></div></div>{importErrors.length > 0 && <div className="max-h-28 overflow-auto rounded-md border p-3 text-sm"><p className="font-medium">Linhas que não serão importadas:</p>{importErrors.map(e => <div key={e.line}>Linha {e.line}: {e.message}</div>)}</div>}{importRows.length > 0 && <div className="min-h-0 overflow-auto rounded-md border"><table className="w-full table-fixed text-sm"><colgroup><col className="w-[12%]" /><col className="w-[17%]" /><col className="w-[28%]" /><col className="w-[13%]" /><col className="w-[16%]" /><col className="w-[14%]" /></colgroup><thead className="sticky top-0 z-10 bg-background"><tr className="border-b text-left"><th className="p-2">Data</th><th className="p-2">Advogado</th><th className="p-2">Cliente</th><th className="p-2">CPF</th><th className="p-2">Telefone</th><th className="p-2">Atendente</th></tr></thead><tbody>{pagedImportRows.map((r, i) => <tr key={`${r.nome_cliente}-${(safeImportPage - 1) * PAGE_SIZE + i}`} className="border-b last:border-0"><td className="p-2 align-top break-words">{br(r.data)}</td><td className="p-2 align-top break-words">{r.advogado}</td><td className="p-2 align-top break-words">{r.nome_cliente}</td><td className="p-2 align-top break-words">{r.cpf || "—"}</td><td className="p-2 align-top break-words">{r.telefone}</td><td className="p-2 align-top break-words">{r.atendente || "—"}</td></tr>)}</tbody></table></div>}</div><DialogFooter className="shrink-0 flex-col gap-2 sm:flex-row sm:items-center sm:justify-between"><StandardPagination current={safeImportPage} total={importTotalPages} totalItems={importRows.length} pageSize={PAGE_SIZE} onChange={setImportPage} /><div className="flex gap-2"><Button variant="outline" onClick={() => setImportOpen(false)} disabled={importing}>Cancelar</Button><Button onClick={confirmImport} disabled={!importRows.length || importing}>{importing ? "Importando..." : `Confirmar importação (${importRows.length})`}</Button></div></DialogFooter></DialogContent></Dialog>
    <AlertDialog open={!!toDelete} onOpenChange={v => !v && setToDelete(null)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Excluir registro?</AlertDialogTitle><AlertDialogDescription>Esta ação não pode ser desfeita. O atendimento será removido permanentemente.</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel>Cancelar</AlertDialogCancel><AlertDialogAction onClick={confirmDelete}>Excluir</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog>
  </div>;
}
